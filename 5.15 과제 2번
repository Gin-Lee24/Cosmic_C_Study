#define _CRT_SECURE_NO_WARNINGS

#include <stdio.h>

int main()
{
	char arr[1000];
	while (1)
	{
		printf("Enter an expession>>");
		fgets(arr,1000,stdin);
		if (arr[0] == 0)
			break;
		int size = sizeof(arr) / sizeof(char);
		int value=0;

		for (int i = 0; i < size - 1; i++)
		{

			if (arr[i] == '+')
			{

			}
			if (arr[i] == '-')
			{

			}
		}
		printf("Value: %d\n", value);
		
	}
	
	return 0;
}
